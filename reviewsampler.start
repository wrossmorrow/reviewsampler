#!/bin/bash 
# reviewsampler.start

echo ${REVIEWSAMPLER_HOME_DIR}
T=$( date +"%FT%T" | sed -n 's/:/-/gp' )
node ${REVIEWSAMPLER_HOME_DIR}/lbserver.js 2> ${REVIEWSAMPLER_HOME_DIR}/error-$T.log > ${REVIEWSAMPLER_HOME_DIR}/info-$T.log & 
echo $! > ${REVIEWSAMPLER_HOME_DIR}/reviewsampler.pid
